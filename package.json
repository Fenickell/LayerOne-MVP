{
  "name": "layerone-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "Aplicacao web da LayerOne para gestao de filamentos, estoque visual e precificacao 3D.",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node local-server.js",
    "dev": "node local-server.js",
    "check": "node --check layerone.js && node --check pricing-engine.js && node --check local-server.js && node --check api/config.js && node scripts/test-pricing-engine.js"
  },
  "engines": {
    "node": ">=20"
  }
}
