{
  "name": "layerone-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "Aplicacao web da LayerOne para gestao de filamentos, estoque visual e precificacao 3D.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check app.js && node --check server.js && node --check api/config.js"
  },
  "engines": {
    "node": ">=20"
  }
}
